# Configuration Variables (Legacy)

This legacy placeholder remains to prevent broken links in existing deployments.

For the current configuration references, use:
- `docs/config/env.md` — environment variables (quick reference)
- `docs/config/unified-config.md` — unified_config.json guide
